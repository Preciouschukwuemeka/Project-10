import React from './node_modules/react';

const Forbidden = () => {
    return(
        <h2 className="validation--errors--label">403 - Oh oh! You can't access this page </h2>
    );
}

export default Forbidden;